/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      backgroundImage: {
        'custom-bg': 'radial-gradient(circle at top, rgba(255,140,0,0.3), transparent 70%), radial-gradient(circle at bottom, rgba(0,120,255,0.3), transparent 70%)',
      },
    },
  },
  plugins: [],
}
